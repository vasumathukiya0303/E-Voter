<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vasu.e_voter">

    <application
        android:allowBackup="true"
        android:icon="@drawable/vote_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVoter">
        <activity android:name=".Admin_Result"></activity>
        <activity android:name=".Admin_Login" />
        <activity android:name=".Do_Vote" />
        <activity android:name=".Voter_Registration" />
        <activity android:name=".Voter_Log_In" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>